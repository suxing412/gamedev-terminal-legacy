node_modules/
.session.json
# 私聊会话档：一席一份（2026-08-31 起）。**只挡 .session.json 是不够的**——
# 那条规则写下时私聊还没落地，而 `git add -A` 当晚就把 .session-总监.json 收进去了。
# 加通配的原因不是洁癖：会话 id 是运行时状态，进了库以后每次说话都会脏一个文件，
# 而「提交只 add 自己改的文件」这条共树纪律就是被这种噪音磨没的。
.session-*.json
# 用量流水：append-only 的运行时账，每说一句话就长一行。同上。
用量.jsonl
# 班次报告：跑一班落一份。**规则要写在功能落地之前**——
# 昨夜 .session-总监.json 就是等它先被 `git add -A` 收进去才想起来的。
班次/
.log
.err

# ── 2026-08-31 补 ──────────────────────────────────────────────
# **exe 绝不入库。**仓根躺着一个 141MB 的 `游戏开发者终端 0.2.2.exe`，
# 而此前 .gitignore 不挡 *.exe——一次 `git add -A` 就会把它永久写进历史，
# 而 git 历史里的大文件是删不掉的（要重写历史）。
# 打包产物的正经去处是 D:/terminal-build/dist。
*.exe

# 情报数据与文稿台的工作目录：都是运行时产物，逐日增长，不该进版本控制
data/
文稿/

# 试验脚本
scratch/

# 自启回执（含本机绝对路径）
自启-回执.json
