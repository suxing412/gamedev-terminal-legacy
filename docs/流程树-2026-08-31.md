# 终端流程树 · UI 巡礼工作清单

2026-08-31｜口径：**逐页、逐部分、逐分支**，遍历必须覆盖所有分支，不许只走 happy path。

图例：`☐` 未过 · `☑` 已过 · `⚠` 已发现问题待改 · `✂` 本轮已改

每一格都要回答同一个第一性问题：**这一格在回答用户的哪个问题？回答得了吗？**
回答不了就重做；回答得了但要滚动/要点开才看得到，也算回答不了
（PRODUCT.md 原则一：三问必须不用滚动、不用点击就能回答）。

---

## 零、壳（所有页签共用，永远在屏上）

| 分支 | 状态 | 备注 |
|---|:--:|---|
| 顶栏 · 徽标 | ☐ | |
| 顶栏 · 页签导航（7 项，当前项高亮） | ☐ | |
| 顶栏 · 顶况（脉灯 / 人闸 / 在跑 / 额度 / 凭据 / 钟） | ✂ | 09-01：「登录 0h」其实是还剩 40 分钟（Math.floor(40/60)）；额度只报周窗口而藏了当下真正会拦人的 5 小时窗；重置时刻占了 31% 宽度。判据 `顶况.test.js` |
| 顶栏 · 形态钮（半屏塔 ⟷ 全屏工作台） | ✂ | 09-01：minWidth:1100 静默钳死 setBounds(460)，**半屏塔从来没窄过**、形态钮落在屏外点不着。判据 `壳内/跑道.js` S18 |
| 凭据横幅（仅临期/过期时出现） | ✂ | 09-01：hidden 被它自己的 display:flex 打败，平时也画着一条 52.6px 高的空琥珀带。补 `.凭幅[hidden]` |
| 左栏 · 等你拍板 · 有单据 | ✂ | 09-01：61 个按钮摊平＝4542px 的墙；36 单里 36 单都逾期＝三档染色全落最深那档（全红等于没红）。按闸位分组＋折叠＋筛选，实测降到 808px。判据 `闸分组.test.js` |
| 左栏 · 等你拍板 · 空 | ☑ | 本来就是分开的两支（「没有等你签的单」/「读不到监制台」） |
| 左栏 · 等你拍板 · 读不到监制台 | ☑ | 「这一栏现在不可信，别拿它当"没事"」——照实说，不折成 0 |
| 左栏 · 议程债（机制类） | ✂ | 09-01：按型分两组（需讨论 10 / 一句话 15）——型＝这个决定要花多少脑子；节名仍逐条显示 |
| 左栏 · 塔况（半屏态才显） | ☐ | |
| 右栏 · 计格 | ☐ | |
| 右栏 · 在跑的 · 有 / 空 | ☐ | |
| 右栏 · 事件流 · 有事件 | ✂ | **40 条同种折成 1 行**（2026-08-31 改） |
| 右栏 · 事件流 · 空 | ✂ | 空态与「读不到」分开了 |
| 右栏 · 事件流 · 读不到 | ☑ | 「读不到流水（因）」，与空态分开 |
| 中栏 · 座条（八席，两席接模型） | ✂ | 09-01：**私聊整条都是皮**——席从没发给服务端，占位符与说注两句都不成立；未接席照样点得进去。现按席分会话、接人设、未接席当场回答为什么。判据 `私聊.test.js` |
| 中栏 · 话流 · 初始 / 对话中 / 流式吐字 | ☐ | |
| 中栏 · 输入区 · 空 / 带单 / 多行 | ☐ | |
| 中栏 · 输入区 · 说注（快捷键提示） | ☐ | |

## 一、对话（`/`）

| 分支 | 状态 | 备注 |
|---|:--:|---|
| 首次进入（欢迎语） | ☐ | |
| 说一句 → 流式回答 | ☐ | |
| 工具调用播报（读/翻/搜/跑/改/写） | ☐ | |
| 坐席报错 | ☐ | |
| 带单上下文 | ☐ | |
| @ 点名某席 | ☐ | |

## 二、日报（`/digest/:d?`）

| 分支 | 状态 | 备注 |
|---|:--:|---|
| 当日有报 | ☐ | |
| 当日无报（空态 + 最近几期） | ☐ | |
| 翻页（上一日 / 下一日 / 到头禁用） | ☐ | |
| 片段模式（壳内） | ☐ | |

## 三、原始流（`/stream/:d?`）

| 分支 | 状态 | 备注 |
|---|:--:|---|
| 有条目 | ☐ | |
| 无条目 | ☐ | |
| 筛选器 · 源 / 档位 / 只看已入报 | ☑ | 三个都实测生效且可叠加（`原始流筛.test.js` 守②） |
| 筛完为空（整组被筛空要收起来） | ✂ | 09-01：**全部组都空时没人接住**，整页除筛选栏一片空白。补空态＋清筛钮；「本来就没数据」不许喊「是筛选挡住的」 |
| 片段模式脚本可重入 | ☑ | 08-31 已修 |

## 四、监视（`/watch`）

| 分支 | 状态 | 备注 |
|---|:--:|---|
| 顶带（瞭望塔 / 监制台 / 今天扶起过谁） | ☐ | |
| 六个监视格 · 好 / 要查 / 不用管 | ✂ | 08-31 致命项：**整页脚本从第 17 行起全不执行**（未声明标识符），这一页是一张会骗人的静止快照。判据 `前端起手.test.js` |
| 清账钮（点了之后的三态） | ✂ | 同上——点了不变灰、不发请求、不报错，因为点击委托压根没注册 |
| 配置读不出 | ☐ | |
| 取不到状态 | ☐ | |

## 五、班次（`/shift`）

| 分支 | 状态 | 备注 |
|---|:--:|---|
| 闸位条（可开班 / 被闸挡） | ☐ | |
| 态谱四族（好 / 在动 / 不用管 / 要查） | ☐ | |
| 今日四班时间带 | ☐ | |
| 有报告 · 拆第一眼 | ☐ | |
| 无报告（空态） | ✂ | 09-01：那句唯一的解释是**死代码**（钟被无条件插在它前面）；(a)没配 /(b)配空了 /(c)读坏了 三种同态。判据 `班次.test.js` 空态①~⑤ |
| 历次列表 · 有 / 无 | ☐ | |
| 报告读不到 | ☐ | |

## 六、席间存照（`/chat`）

| 分支 | 状态 | 备注 |
|---|:--:|---|
| 坐席名单（八席，未接的压暗） | ☐ | 片段里已能出现（08-31 修） |
| 条目列表 · 播报 / 产出物 | ✂ | 09-01 三件：整页时刻偏八小时（切 ISO 串）、开屏落在六天前那条而最新的在 14000px 之下、左栏发言人点不动。判据 `存照.test.js` S14 |
| 展开全文 / 收起 | ☐ | |
| 末行陈旧提示 | ✂ | 09-01：只报「写了一半」，不报「写完了但三天没人说话」。补 静默多久() |
| 空 / 读不到 | ☐ | |

## 七、文稿（`/doc`）—— 本轮重点

| 分支 | 状态 | 备注 |
|---|:--:|---|
| 文件库 · 按类别分组 | ✂ | 08-31 从「按仓」改成「按用途」 |
| 文件库 · 筛选（有记号 / 可写 / 类别 / 清筛） | ✂ | 08-31 新增 |
| 文件库 · 条目两行式（名 / 日 / 出处 / 记号 / 体积） | ✂ | 08-31 改，去掉 rtl 截断 |
| 文件库 · 搜文件名 | ☑ | data-路 是完整相对路径，本地过滤对整串 indexOf（比 placeholder 宣传的强） |
| 文件库 · 搜正文（回车） | ✂ | 09-01 两处：**静默作废已开的筛而钮还亮着**；**400 条读预算按根表序耗尽，记忆库那 71 份永远搜不到**（「雷火」3→0）。判据 `文稿筛.test.js` / `文稿.test.js` 守㉕ |
| 文件库 · 筛完为空 | ✂ | 08-31 补了 .稿筛空 + 清筛钮 |
| 文件库 · 收起/展开 | ☐ | |
| 阅读态 · 有记号（记号栏） | ☐ | |
| 阅读态 · 无记号（一行说明） | ☐ | |
| 阅读态 · 只读（编辑钮换成只读徽章） | ☐ | |
| 阅读态 · 读不到 | ☐ | |
| 编辑态 · 分栏（宽 / 窄两种） | ☐ | |
| 编辑态 · 预览段落记号钮（hover 才亮） | ☐ | |
| 编辑态 · 工具条状态（已就绪/未保存/草稿已存/连败/锁况） | ☐ | |
| 编辑态 · 草稿恢复问答（同源 / 不同源两种文案） | ☐ | |
| 编辑态 · 冲突框（四选项 + 差异图 + 无 base 警告） | ☐ | |
| 编辑态 · 退出确认（草况按记录说话） | ☐ | |
| 编辑态 · 切页签确认 | ☐ | |
| 自绘问答框（问 / 告 两形） | ✂ | 08-31 新增，替掉原生 alert/confirm |
| 版本历史 | ✂ | 08-31 补入口；09-01 补「和当前比」「用这一版覆盖编辑器」——**看得到还要拿得回来**。判据 `文稿筛.test.js` 史①~④ |

## 八、半屏情报塔形态

| 分支 | 状态 | 备注 |
|---|:--:|---|
| 切换动作本身 | ✂ | 09-01：minWidth 钳死 setBounds。跑道实测 innerWidth 446、形态钮右缘 422（在窗内） |
| 半屏下的左栏 | ☐ | |
| 半屏下的塔况 | ☐ | |
| 半屏下各页签（**七个页签在 360px 宽里怎么放？**） | ☑ | 塔态下整个收起——视图区长在话栏里而话栏在塔态是 display:none，留着一个点了不动的导航比没有导航坏 |

---

## 本轮已认定要改的（发现即记，逐条销）

1. ✂ 右栏事件流：40 条同种折成 1 行 + 空态与「读不到」分开
2. ✂ 文稿库：按用途分类（工单留痕 454/951 = 48% 不再淹没在办的 50 份）
3. ✂ 文稿库：有记号 / 可写 / 类别三条可叠加的筛选
4. ✂ 文稿库：条目两行式，去掉 `direction: rtl` 的头部截断
5. ✂ 文稿库：筛完为空的空态（08-31）
6. ✂ 版本历史：入口（08-31）＋ 对比与覆盖（09-01）
7. ✂ 半屏形态：页签整个收起；而**真正的问题是塔从来没窄过**（minWidth 钳死 setBounds）
8. ✂ 座条：不只是「看不出区别」——**私聊整条都是皮**，席从没发给服务端

---

## 09-01 夜 · 内部评审（18 个 agent，五攻面 → 逐条复核）的处置

F1、F2 两条致命项当夜修掉（F2 距起火四分钟）。S1–S19 与 M1–M9 全部落地，
逐条对应见 git log。这一夜反复撞到的**同一族病**，值得单独记住：

| 族 | 次数 | 形状 |
|---|:--:|---|
| **静止的活人** | 6+ | 页面在、点得动、数据到、不报错，什么都没发生 |
| **屏上说了没发生的事** | 5 | 私聊「有自己的记忆」、冲突框「可以找回」、「N 条在等你」配空列表、亮着的筛选钮、「登录 0h」 |
| **静默截断** | 3 | 搜索读预算、单根上限、事件窗口——都是「悄悄变小的真数」 |
| **拿 ISO 串当钟面** | 4 | /api/events 的月、事件行的日期、监视页的「取于」、存照页整页 |
| **一个概念存 N 份** | 2 | 事种/折叠存了四份（只治了两份）、滚动条两份 |
| **判据在验空气** | 6 | 函数存在≠被调用、indexOf 找到注释、语料分辨不出封顶、量早了、没造 sticky activation、掏空归类仍全绿 |

最后一行是这一夜最该记住的：**判据本身也会撒谎，而且撒得比代码更难发现**——
它一直是绿的。test/自证能红.js（173 处变异）是唯一挡得住这一族的东西。

## 剩下的（未销分支怎么读）

上表里仍标 ☐ 的分支，**不等于「有问题」**，而是「这一轮没有逐一走过」。
本轮的走法是：先按内部评审那 28 条实测出来的问题逐条销，再回头补面。
面上还没逐格走过的主要是——对话页六支（工具播报/坐席报错/带单/@点名）、
日报四支、监视页顶带与三种读不到态、班次页态谱与历次、编辑态那十一支。
编辑态那一组现在有了 test/壳内/跑道.js 这条跑道可以逐格量，是下一轮成本最低的一段。
