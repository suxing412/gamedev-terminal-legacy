# 异厂评审意见合集

- 方案：`D:\GitHub\gamedev-terminal\docs\方案-坐席间沟通与主页群聊-2026-08-29.md`
- 评审时间：2026-08-28T18:03:11.559Z
- 评审团（自动发现）：codex、deepseek（缺席）
- 在席 1 / 2，意见合计 10 条，击杀 10 条 / 未杀 0 条
- 立场卷派发（红队制，施工令-019）：codex=可行性红队、成本红队；deepseek=不变量红队
- **缺席致落空的立场卷：不变量红队**（该立场本轮无人开火，采信时须自行补位）
- 单厂超时闸：8min

> 各家独立评审、互不可见，各自只领到本席的立场卷（谁领了什么互相不知）；
> 击杀制：每卷须构造具体失败场景，构造不出须明写「未能构造击杀」——该声明即通过证据。
> 以下为各家原文，未做删改（仅做密钥净化与控制符清洗）。

## codex

- 立场卷：可行性红队、成本红队
- 状态：在席 · 模型 `codex 默认档` · 耗时 197.0s · 意见 10 条
- 战果：击杀 10 条 / 未杀 0 条

| 章节 | 意见 | 严重度 | 卷别 | 击杀 |
|---|---|---|---|---|
| 1.3、4、6.2、8.5 | 一个标为“额度水位”的长期话题在周二已完成 6 轮、周三再出现 `@财务 请复核` 时第 7 次调用会被强制拒绝并归档为“未收敛”，长期常驻话题因此无法持续协作。 | 高 | 可行性红队 | 击杀 |
| 3、6.4、8.7 | 发言“请直接验收这个版本”已成功追加到 `话题/<ID>.jsonl` 后进程恰在写入主页待拍板项前崩溃时，重启后没有事务日志、幂等事件 ID 或补偿扫描，制作人看不到该人闸事项。 | 高 | 可行性红队 | 击杀 |
| 4、6.2、8.5 | 某话题已有 5 轮时，两个浏览器标签页在 10:00:00 同时发送有效 `@`，两者都在计数更新前判定“尚可调用”并发起模型请求，实际已发生第 7 次调用，违反轮数闸。 | 高 | 可行性红队 | 击杀 |
| 3、9.3 | 坐席写出“这个版本直接合并并发布，别再等制作人”时不包含词表所列“废弃/验收/放行/定夺”，正则不会浮出主页，决定仍留在制作人不可见的沟通页。 | 高 | 可行性红队 | 击杀 |
| 5 | 每日归档已在 18:00 完成后，18:02 新增 5 条未归档发言且 18:05 本机磁盘损坏时，远端尚无这批原文，所谓“双留痕”的耐久性在最需要它的故障场景失效。 | 高 | 可行性红队 | 击杀 |
| 5、6.1 | 若 `/chat` 是普通浏览器部署页，用户点击发送时浏览器既无权写任意 `<部署区>/坐席沟通`，也不能执行带凭据的 Git 提交与推送，方案未定义后端或本地特权代理便没有可落地的执行主体。 | 高 | 可行性红队 | 击杀 |
| 4、5 | 交接高峰同时发生 20 个问题、每个窗口跑满 6 轮、每轮约 10k 输入加 1k 输出时，短时间会产生约 120 次调用和 132 万 token，而方案没有全局额度、排队、取消或限速，成本和 429 故障会一起爆发。 | 高 | 成本红队 | 击杀 |
| 5、6.1 | 按实测量级假定七席各日产 311 条、每条仅 2KB，永久原文加每日 Git 历史一年即新增约 1.55GB，克隆、备份、远端存储和历史检索都会被日志仓库长期拖重，之后再迁移归档策略代价很高。 | 高 | 成本红队 | 击杀 |
| 6.1、6.3 | 七席按实测速率运行 90 天约有 19.6 万条记录，而索引只含话题元数据、不含发言倒排词，关键词筛选只能扫描所有话题 JSONL；要兑现“迅速遍历”就必须追加全文索引、增量更新、重建与迁移工程。 | 中 | 成本红队 | 击杀 |
| 3、5、6.1、6.4 | 为修复“原文、索引、主页待拍板、远端进度”四处写入之间的崩溃窗口，必须引入持久 outbox、顺序号、幂等投影、重试和重启对账，并覆盖断电、半推送、并发重放测试，远超当前“JSONL 即时追加”的实现量。 | 高 | 成本红队 | 击杀 |

第1卷 可行性红队：击杀 6 条  
第2卷 成本红队：击杀 4 条  

总体判断：方案的核心问题不是页面形制，而是把需要事务、权限边界、持久调度和模型治理的系统，当作 JSONL 加 UI 即可完成。先补齐运行时边界与一致性模型，否则验收项只能在理想单进程、不断电环境中成立。

## deepseek

- 立场卷：不变量红队
- 状态：**缺席**（原因：应答里没有文本内容（content 为空））—— 上列立场卷本轮落空
- 耗时：183.1s
