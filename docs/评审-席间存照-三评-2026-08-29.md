# 异厂评审意见合集

- 方案：`D:\GitHub\gamedev-terminal\docs\方案-席间存照-2026-08-29.md`
- 评审时间：2026-08-28T18:42:32.717Z
- 评审团（自动发现）：codex、deepseek（缺席）
- 在席 1 / 2，意见合计 9 条，击杀 9 条 / 未杀 0 条
- 立场卷派发（红队制，施工令-019）：codex=可行性红队、成本红队；deepseek=不变量红队
- **缺席致落空的立场卷：不变量红队**（该立场本轮无人开火，采信时须自行补位）
- 单厂超时闸：8min

> 各家独立评审、互不可见，各自只领到本席的立场卷（谁领了什么互相不知）；
> 击杀制：每卷须构造具体失败场景，构造不出须明写「未能构造击杀」——该声明即通过证据。
> 以下为各家原文，未做删改（仅做密钥净化与控制符清洗）。

## codex

- 立场卷：可行性红队、成本红队
- 状态：在席 · 模型 `codex 默认档` · 耗时 314.1s · 意见 9 条
- 战果：击杀 9 条 / 未杀 0 条

| 章节 | 意见 | 严重度 | 卷别 | 击杀 |
|---|---|---|---|---|
| §2.1 条目 ID | 同一坐席在 `2026-08-29T02:21:04.000Z` 因重试写入两条完全相同的“收到”时，`t`、`from`、`text` 全同而 ID 必然相同，导致两条凭据无法被分页锚点或前端键值区分，H104 的“零碰撞”当场失败；需写侧提供不可变序号或字节偏移。 | 高 | 可行性红队 | 击杀 |
| §2.3 对话流／§4.6 分页 | 文件初始有 `m1…m200`、首屏取 `m141…m200` 后恰好追加 `m201…m260`，方案未定义快照或 cursor 语义，按常规 `offset=60` 向上加载会再次得到 `m141…m200`，首轮历史加载全重复且验收用例抓不到。 | 高 | 可行性红队 | 击杀 |
| §2.6 半行与并发读 | 写方 A 将一条 JSON 分两次写入，A 的前半段与后半段之间被写方 B 插入一整条记录时，读者看到的是“中间坏行”而非末行半行，按既定规则永久计坏并丢掉 A、B 两条完整业务记录；“等下轮”不会修复已成形的交错字节。 | 高 | 可行性红队 | 击杀 |
| §1.2 三型／§2.5 播报去处／§6.5 已知薄弱处 | 制作人发送 `from=制作人, text=排期复判：TK-312 应回滚，先不要合并` 时，四个写死前缀会把一条有明确说话对象的对话归为播报并排除，直接违反“一条不藏”；仅有 `t/from/text` 无法从数据上证明它不是播报。 | 高 | 可行性红队 | 击杀 |
| §4.13 旧路径不 404 | 发布后直接请求 `GET /chat` 时，范围把 `/chat` 定义为本页，验收又要求同一路径返回 302 到一个未声明的新路径；一个响应不能同时是本页 200 与未知目标的 302，路由及自动验收均无从实现。 | 高 | 可行性红队 | 击杀 |
| §2.4 附件标记与索引 | 在 4,999 条记录中，唯一附件位于最早第 1 条、最新 60 条均无附件时，用户点“只看有附件”只会得到空结果，必须再人工向上加载 83 页才能发现它；所谓“索引”把一次查档变成线性翻页，最终仍要补全局检索接口。 | 高 | 成本红队 | 击杀 |
| §2.3 对话流／§2.4 零额外请求 | 最近 60 条各含 12 MB 多行测试日志时，虽只显示六行，因“展开零额外请求”仍须在首屏携带完整 720 MB 正文；20 MB 红线只停筛选、不限制传输，反向代理超时和浏览器内存问题会迫使新增正文按需接口或内容存储改造。 | 高 | 成本红队 | 击杀 |
| §2.5 播报去处／§2.7 规模红线 | 在 4,999 条、19.9 MB 的日志上，每次首屏都要给出全历史精确播报数 `N`，而方案没有计数器、索引或缓存失效协议，只能扫描全文件；若 100 人每分钟打开一次，即约解析 1.99 GB/分钟、119 GB/小时，缓存化本身变成一项并发一致性工程。 | 高 | 成本红队 | 击杀 |
| §2.7 筛选与规模红线／§5 明确不做 | 已有 4,999 条小记录时，正常追加第 5,001 条即使文件仅约 5 MB 也会关闭发言人、附件和关键词三种筛选；用户最需要查档时核心能力被切断，而修复所需的倒排索引已被明确移出范围，必然形成上线后的紧急返工单。 | 高 | 成本红队 | 击杀 |

第1卷 可行性红队：击杀 5 条  
第2卷 成本红队：击杀 4 条  

总体判断：本稿的“附件不藏”只缓解了误标记，未解决记录唯一性、并发写入、播报可判定性和分页一致性。应先补齐写入协议、稳定分页契约、消息类型字段与规范路由，再谈页面实现和规模阈值。

## deepseek

- 立场卷：不变量红队
- 状态：**缺席**（原因：应答里没有文本内容（content 为空））—— 上列立场卷本轮落空
- 耗时：300.2s
