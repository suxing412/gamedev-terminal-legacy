# 异厂评审意见合集

- 方案：`D:\GitHub\gamedev-terminal\docs\方案-席间存照-2026-08-29.md`
- 评审时间：2026-08-28T18:56:04.346Z
- 评审团（自动发现）：codex、deepseek（缺席）
- 在席 1 / 2，意见合计 9 条，击杀 9 条 / 未杀 0 条
- 立场卷派发（红队制，施工令-019）：codex=可行性红队、成本红队；deepseek=不变量红队
- **缺席致落空的立场卷：不变量红队**（该立场本轮无人开火，采信时须自行补位）
- 单厂超时闸：8min

> 各家独立评审、互不可见，各自只领到本席的立场卷（谁领了什么互相不知）；
> 击杀制：每卷须构造具体失败场景，构造不出须明写「未能构造击杀」——该声明即通过证据。
> 以下为各家原文，未做删改（仅做密钥净化与控制符清洗）。

## codex

- 立场卷：可行性红队、成本红队
- 状态：在席 · 模型 `codex 默认档` · 耗时 352.6s · 意见 9 条
- 战果：击杀 9 条 / 未杀 0 条

| 章节 | 意见 | 严重度 | 卷别 | 击杀 |
|---|---|---|---|---|
| §2.3、§2.4 | 当附件是第 1 行 `验收报告 TK-312\n……`、首屏只载入 m266–m325 时，索引能列出它但正文并未在客户端，对该项“展开零额外请求”只能失败或暗中改为翻页/按 ID 拉取；应明确索引携带正文或提供按行序定向加载。 | 高 | 可行性红队 | 击杀 |
| §2.5.1、§4.11 | 夹具仅有一条命中播报 `{from:"项管",text:"排期复判：……"}` 时默认态 N=1、折叠数=1，但点击“展开例行播报”后 N 仍应为 1 而实际折叠数=0，验收项“N 等于被折叠的条数”没有可同时满足的实现。 | 高 | 可行性红队 | 击杀 |
| §2.6、§4.9 | 一条末行从 10:00 开始写入且每 4 分 59 秒又写入一个字节、始终不闭合时，它已不完整超过 5 分钟，但文件 mtime 永远不足 5 分钟，而坏行本身又没有可靠的写入起点；页面会永久“等下轮”，应持久记录首见时间或引入写入事务标记。 | 高 | 可行性红队 | 击杀 |
| §2.1、§2.4、§2.6 | 中间第 212 行若为合法 JSON `{"t":"2026-08-29T02:14:00Z","from":"项管","text":null}`，它不会命中“解析失败的坏行”，但 `sha1(text)`、首行判别和逻辑行统计都不能执行，整页可能 500；方案只有字段实测、没有类型契约或结构坏行策略。 | 高 | 可行性红队 | 击杀 |
| §2.3、§4.6 | 首屏 m266–m325 时滚动观察器在首个请求返回前触发两次，两个 `beforeLine=266` 请求都会返回 m206–m265，行序游标并不能阻止重复插入，React key 冲突后会出现重复或缺条；应规定单飞请求、取消旧请求及按行序幂等合并。 | 中 | 可行性红队 | 击杀 |
| §2.3、§2.4 | 在 19.9 MB 文件中放入 200 个各 99 KB 的附件且最早附件不在首屏时，若坚持“展开零额外请求”，首屏必须预送近 20 MB 附件正文，分页的传输上限被架空；若不预送，就必须补取件接口、缓存和失败处理，属于未预算的架构分叉。 | 高 | 成本红队 | 击杀 |
| §2.7、§4.7 | 文件为 19.9 MB、用户连续执行 12 次关键词筛选时，无倒排索引意味着至少 12 次全量文本扫描，累计约 239 MB 的字符串处理仍在“筛选必须可用”的红线内；没有性能基准、目标机器或防抖/worker 方案，阈值前的性能取证和返工被整体后置。 | 高 | 成本红队 | 击杀 |
| §2.5.1、§六.5 | 后续新增播报前缀“日报”时，历史中 `{from:"项管",text:"日报请总监在 15:00 前签 TK-312"}` 与真正日报在现有字段上不可区分，新增词会把直接指令默认折叠；由于标记读时计算且无人工覆盖，安全上线只能人工复核所有命中历史，且每次扩词都要重做。 | 中 | 成本红队 | 击杀 |
| §2.1、§2.7、§七 | 文件达到 20.01 MB 后方案主动关闭三项筛选，若要恢复“查得到”就需补索引并处理行序 ID 在分卷/轮转后的历史映射；这不是优化而是一次带数据迁移与兼容验证的重做，且没有预留预算、迁移格式或双读期。 | 高 | 成本红队 | 击杀 |

第1卷 可行性红队：击杀 5 条  
第2卷 成本红队：击杀 4 条  

总体判断：本稿修复了“分类即隐藏”的主问题，但分页、附件展开和播报折叠的边界尚未闭合。尤其“零额外请求”与只载入 60 条不能同时作为真实实现承诺。

## deepseek

- 立场卷：不变量红队
- 状态：**缺席**（原因：应答里没有文本内容（content 为空））—— 上列立场卷本轮落空
- 耗时：233.6s
