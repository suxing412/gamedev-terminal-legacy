# 异厂评审意见合集

- 方案：`D:\GitHub\gamedev-terminal\docs\方案-席间存照-2026-08-29.md`
- 评审时间：2026-08-28T18:48:26.686Z
- 评审团（自动发现）：codex、deepseek（缺席）
- 在席 1 / 2，意见合计 8 条，击杀 8 条 / 未杀 0 条
- 立场卷派发（红队制，施工令-019）：codex=可行性红队、成本红队；deepseek=不变量红队
- **缺席致落空的立场卷：不变量红队**（该立场本轮无人开火，采信时须自行补位）
- 单厂超时闸：8min

> 各家独立评审、互不可见，各自只领到本席的立场卷（谁领了什么互相不知）；
> 击杀制：每卷须构造具体失败场景，构造不出须明写「未能构造击杀」——该声明即通过证据。
> 以下为各家原文，未做删改（仅做密钥净化与控制符清洗）。

## codex

- 立场卷：可行性红队、成本红队
- 状态：在席 · 模型 `codex 默认档` · 耗时 194.3s · 意见 8 条
- 战果：击杀 8 条 / 未杀 0 条

| 章节 | 意见 | 严重度 | 卷别 | 击杀 |
|---|---|---|---|---|
| §2.7、§4 验收 7 | 输入 5001 条、每条正文 20 字的夹具（文件不足 1 MB）时，§2.7 要求低于 20 MB 筛选可用而验收 7 要求筛选停用，任一实现必然失败其中一条验收。 | 高 | 可行性红队 | 击杀 |
| §2.7、§4 验收 13 | 对 `GET /chat`，§2.7 要求该路径直接承载本页（200），验收 13 却要求它返回到“新路径”的 302；同一请求不能同时满足，且方案根本未定义新路径。 | 高 | 可行性红队 | 击杀 |
| §2.5 | 输入 `{from:"项管", text:"排期复判：TK-312 的回执请总监今天签字"}` 时，它满足“项管 + 前缀”而被排除，但这是明确指向总监的对话；现有数据只有 `t/from/text`、没有收件人或消息类型，规则无从区分，必然会静默藏掉真对话。 | 高 | 可行性红队 | 击杀 |
| §2.6 | 末行写到 `{"t":"2026-08-29T10:00:00+08:00","from":"总监","text":"TK-312` 后写进程因磁盘满或崩溃而永久停止时，规则会把它一直当“半行”等待下轮，页面既不报坏行也不提示数据缺失，存照永久不可查。 | 高 | 可行性红队 | 击杀 |
| §1.1、§2.4、§6.1 | 一年后 6243 条历史中加入词“方案”以补捞产出物时，所有首行是“方案”且后续为普通讨论的旧消息会立刻变成附件；读时重算确实免迁移，却没有人工裁决、版本或覆盖机制，必须逐批回看历史候选才能维持索引可信度。 | 高 | 成本红队 | 击杀 |
| §2.3、§4 验收 3 | 输入一条没有换行的 3000 字中文正文时，验收要求“截到 6 行”，但方案未定义“行”是 `\\n` 逻辑行还是依赖字体、窗口宽度和浏览器的视觉行；前者不会折叠，后者需要固定浏览器/字体/视口及视觉回归测试，机器可判验收会拖成跨端调试。 | 高 | 成本红队 | 击杀 |
| §2.3、§2.4、§2.7、§6.2-6.3 | 输入 250 条各约 80 KB 的正文（约 19.5 MB，单条仍未触发 100 KB 信号）时，打开页仍须全文件扫附件、默认 60 条可能携带约 4.8 MB 正文，而筛选尚未停用；当前只测 150 KB 实测和 200 条夹具，未定义响应体、延迟或内存预算，性能问题会在接口和前端完成后才暴露并迫使重做分页/摘要契约。 | 高 | 成本红队 | 击杀 |
| §2.5、§6.5、§7 | 新增例行播报前缀“交付延后”且项管每天产生 17 条这类消息时，它们会混入对话流；每新增前缀都要人工发现、改硬编码、补历史回归夹具，并重新核对底部 N，播报语义没有结构化来源，维护工作会随运营词汇持续累积。 | 中 | 成本红队 | 击杀 |

第1卷 可行性红队：击杀 4 条  
第2卷 成本红队：击杀 4 条  

总体判断：本稿尚不能进入实现。两条验收标准彼此矛盾，而播报排除与末行容错会在合法输入下直接造成记录不可见；即使修正功能矛盾，启发式分类与未量化的近阈值性能仍会形成持续返工面。

## deepseek

- 立场卷：不变量红队
- 状态：**缺席**（原因：应答里没有文本内容（content 为空））—— 上列立场卷本轮落空
- 耗时：167.9s
