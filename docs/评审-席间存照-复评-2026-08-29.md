# 异厂评审意见合集

- 方案：`D:\GitHub\gamedev-terminal\docs\方案-席间存照-2026-08-29.md`
- 评审时间：2026-08-28T18:33:43.376Z
- 评审团（自动发现）：codex、deepseek（缺席）
- 在席 1 / 2，意见合计 9 条，击杀 9 条 / 未杀 0 条
- 立场卷派发（红队制，施工令-019）：codex=可行性红队、成本红队；deepseek=不变量红队
- **缺席致落空的立场卷：不变量红队**（该立场本轮无人开火，采信时须自行补位）
- 单厂超时闸：8min

> 各家独立评审、互不可见，各自只领到本席的立场卷（谁领了什么互相不知）；
> 击杀制：每卷须构造具体失败场景，构造不出须明写「未能构造击杀」——该声明即通过证据。
> 以下为各家原文，未做删改（仅做密钥净化与控制符清洗）。

## codex

- 立场卷：可行性红队、成本红队
- 状态：在席 · 模型 `codex 默认档` · 耗时 243.6s · 意见 9 条
- 战果：击杀 9 条 / 未杀 0 条

| 章节 | 意见 | 严重度 | 卷别 | 击杀 |
|---|---|---|---|---|
| §3.3、§4.10（条目 ID） | 历史 jsonl 若有两条 `speaker=项管、at=2026-08-29 02:20` 且均无 `id` 字段，方案没有 ID 生成、持久化或迁移规则，模型无法返回可验证且长期稳定的条目 ID，只能不出卡，存照引用主功能无法实现。 | 高 | 可行性红队 | 击杀 |
| §2.3、§3.3（原始记录读取） | 写入进程在 `12:00:00.004` 只写完新 jsonl 行 `{"id":"m326","content":"收口...` 的前半段，页面同时全量读取时既无快照、锁也无尾行重试规则，结果要么整页误报“读不到”、要么遗漏最新凭据，正好在机器活动时失去可查性。 | 高 | 可行性红队 | 击杀 |
| §1.1、§2.3（产出物分型） | 输入一条独立产出 `验收报告 TK-312\n结论：通过…`，它不含四个写死标记，规则必将其放入对话流；制作人勾选“只看有附件”时找不到已交付的报告，直接失败于“他交出来的东西是什么”。 | 高 | 可行性红队 | 击杀 |
| §3.3、§6.5（主页接线） | 主页群聊收到“`TK-146 何时收口？`”且原始记录存在唯一工单号时，方案仍未定义检索由哪个处理器触发、ID 如何返回群聊、卡片由哪个组件插入；文档自己承认接线未定，因此实现按本文无法产出卡片。 | 高 | 可行性红队 | 击杀 |
| §3.2、§4.10（逐字节原文） | jsonl 原始字节为 `{"id":"m7","正文":"甲\\n乙"}` 时，卡片若显示真实换行就不可能与 jsonl 原始字节相同，若显示反斜杠和 `n` 又不是当时说的话；未定义“解析后字段值”还是“原始序列化字节”的规范，验收断言不可正确实现。 | 中 | 可行性红队 | 击杀 |
| §3.3、§3.4（无索引检索） | 按方案当前量级取 `325` 条、`300 KB` UTF-8 对话，每次主页提问若让模型选 ID 就需提供约十万级中文字符的候选语料；每天 `100` 次提问即约千万级输入 token，若改为代码预筛又必须补建方案明确未设计的候选检索层。 | 高 | 成本红队 | 击杀 |
| §2.3、§2.4、§五（首屏全量下发） | 输入 `4,999` 条、`19.9 MB` 的文件时仍未越过闸门，20 人各刷新 10 次便传输约 `3.98 GB`，浏览器还必须解析并保有全部 `<details>` 正文；筛选停用并未减少首屏传输，事后只能返工分页或按需正文协议。 | 高 | 成本红队 | 击杀 |
| §1.1、§六.1（词表演进） | 第三周出现“验收报告”漏判后即使只新增这一个标记，也必须回查既有 325 条及后续历史，区分真正报告与引用“验收报告”的普通答复；方案未保存人工分型证据，词表每次扩展都要重做审计、回归和存照链接核验。 | 高 | 成本红队 | 击杀 |
| §3.3、§六.2（相关性与时序） | 记录 `m42=“TK-146 在 0.23.15 修复”`、`m108=“TK-146 在 0.23.15 回滚待复核”` 同时存在时，提问“`TK-146 在 0.23.15 修好了吗？`”会满足两关键词规则却命中相反结论；没有时序、排序或冲突策略，只能靠人工持续裁定，且没有反馈数据来降低后续介入频次。 | 高 | 成本红队 | 击杀 |

第1卷 可行性红队：击杀 5 条  
第2卷 成本红队：击杀 4 条  

总体判断：方案仍不具备实施放行条件。先补齐不可变记录身份、读取一致性和主页接线；随后重新设计检索与首屏载荷边界，否则“存照”会在最需要取证和规模增长时失效。

## deepseek

- 立场卷：不变量红队
- 状态：**缺席**（原因：应答里没有文本内容（content 为空））—— 上列立场卷本轮落空
- 耗时：155.5s
