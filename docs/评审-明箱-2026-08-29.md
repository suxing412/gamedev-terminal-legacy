# 异厂评审意见合集

- 方案：`D:\GitHub\gamedev-terminal\docs\方案-明箱-2026-08-29.md`
- 评审时间：2026-08-28T18:26:21.054Z
- 评审团（自动发现）：codex、deepseek（缺席）
- 在席 1 / 2，意见合计 7 条，击杀 7 条 / 未杀 0 条
- 立场卷派发（红队制，施工令-019）：codex=可行性红队、成本红队；deepseek=不变量红队
- **缺席致落空的立场卷：不变量红队**（该立场本轮无人开火，采信时须自行补位）
- 单厂超时闸：8min

> 各家独立评审、互不可见，各自只领到本席的立场卷（谁领了什么互相不知）；
> 击杀制：每卷须构造具体失败场景，构造不出须明写「未能构造击杀」——该声明即通过证据。
> 以下为各家原文，未做删改（仅做密钥净化与控制符清洗）。

## codex

- 立场卷：可行性红队、成本红队
- 状态：在席 · 模型 `codex 默认档` · 耗时 194.8s · 意见 7 条
- 战果：击杀 7 条 / 未杀 0 条

| 章节 | 意见 | 严重度 | 卷别 | 击杀 |
|---|---|---|---|---|
| 3.3 附件；6.3 | 输入一条 `收口报告 TK-146`、正文 3013 字：收起态要求 HTML 不含正文，但方案没有点击后按消息 ID 读取正文的接口或载荷，客户端点击时无处取得内容，附件将只能显示标题而打不开。 | 高 | 可行性红队 | 击杀 |
| 3.1 坐席名单；6.5 | 状态为某已接模型席在页面渲染后 5 秒断连：方案只说明要显示“今天的实况”，却未定义连接状态的数据源、读取权限或刷新语义，页面无法可靠判定实心/空心灯，验收中的“五未接、二已接”也无法稳定复现。 | 高 | 可行性红队 | 击杀 |
| 1；3.2–3.3；6.4 | 输入一条普通说明、正文 3000 字且不属于报告：分型规则把所有 `>800 字`直接归为附件并默认收起，而验收又要求 3000 字“消息”默认截到 6 行，单一输入会同时要求“附件收起”和“对话正文截断”，验收条件互相冲突。 | 高 | 可行性红队 | 击杀 |
| 1；7.1–7.3 | 输入一条 650 字、标题为“TK-146 验收说明”的交付报告，正文不含“收口报告/漏洞清单/完工报告”：它既不命中前缀也未超 800 字，只能落入中间态并被放进对话流；没有权威类型字段或可持久化人工标注，方案无法实现“报告型出附件”的语义要求。 | 高 | 可行性红队 | 击杀 |
| 1；7.2 | 已知 325 条中有 12 条中间态；按同一比例增长到 20,000 条会产生约 738 条待判项，而方案既无人工归类流程也无保存归类结果的数据结构，规则每次调整都要重扫、重判历史，所谓纯呈现会演变为持续人工运营。 | 高 | 成本红队 | 击杀 |
| 3.4；5 | 输入规模为 50,000 条、平均每条 3 KB（约 150 MB），10 人各输入 8 个关键词字符进行筛选：不建索引且未规定分页、缓存或防抖时，服务端要反复解析扫描约 12 GB 数据；若改为前端筛选则首屏须下发约 1.5 GB，筛选功能会拖出查询架构和性能治理工期。 | 高 | 成本红队 | 击杀 |
| 标题范围 `/chat`（拟更名）；2；6.9 | 部署时若“拟更名”落实为路径从 `/chat` 改为新路径，而现有导航、书签或脚本仍请求 `/chat`，用户直接落到旧页或 404；方案只约束页签和标题同源，未列路由兼容、重定向、链接清查和回归范围，命名改动会扩成跨页迁移。 | 中 | 成本红队 | 击杀 |

第1卷 可行性红队：击杀 4 条

第2卷 成本红队：击杀 3 条

总体判断：方案把核心数据契约、展开读取机制和状态来源留成空白，同时验收存在直接矛盾；在补齐这些前，不应以“纯呈现改动”估工或开工。

## deepseek

- 立场卷：不变量红队
- 状态：**缺席**（原因：应答里没有文本内容（content 为空））—— 上列立场卷本轮落空
- 耗时：145.9s
