# 异厂评审意见合集

- 方案：`D:\GitHub\gamedev-terminal\docs\方案-监视器归一与可视化-2026-08-29.md`
- 评审时间：2026-08-28T16:47:07.063Z
- 评审团（自动发现）：codex、deepseek（缺席）
- 在席 1 / 2，意见合计 8 条，击杀 8 条 / 未杀 0 条
- 立场卷派发（红队制，施工令-019）：codex=可行性红队、成本红队；deepseek=不变量红队
- **缺席致落空的立场卷：不变量红队**（该立场本轮无人开火，采信时须自行补位）
- 单厂超时闸：8min

> 各家独立评审、互不可见，各自只领到本席的立场卷（谁领了什么互相不知）；
> 击杀制：每卷须构造具体失败场景，构造不出须明写「未能构造击杀」——该声明即通过证据。
> 以下为各家原文，未做删改（仅做密钥净化与控制符清洗）。

## codex

- 立场卷：可行性红队、成本红队
- 状态：在席 · 模型 `codex 默认档` · 耗时 104.8s · 意见 8 条
- 战果：击杀 8 条 / 未杀 0 条

| 章节 | 意见 | 严重度 | 卷别 | 击杀 |
|---|---|---|---|---|
| 4.2 `监视器.json` / 4.3 `/watch` 页 | 当瞭望塔把同一事件写成带 BOM 的 UTF-16 日志、轮转后暂时不存在，或 JSONL 中出现半行时，方案未定义三个“输出文件”的编码、原子写入、轮转和字段契约，终端无法可靠归一，只能误报“读不到”或整格解析失败。 | 高 | 可行性红队 | 击杀 |
| 4.4「终端跟着主机走」 | 在 Windows 登录任务以部署前旧路径 `D:\release v1\终端.exe` 注册、升级后目录改为 `D:\release v2\终端.exe` 时，计划任务仍成功存在却启动旧版本或直接找不到文件；方案仅要求“任务存在且指向部署区 exe”，没有任务账户、工作目录、升级替换和失败取证机制，无法保证“开机即有”。 | 高 | 可行性红队 | 击杀 |
| 4.5 自监：谁看着看守者 | 当 `<部署区>\瞭望塔` 所在盘在 10:00 被拔掉或 ACL 改为拒绝读取时，终端读不到塔心跳、监制台也读不到同一心跳文件；两层同时失明，所谓“三者互为观察者且没有两条走同一路”被共享文件路径击穿，无法区分塔死、磁盘死和权限死。 | 高 | 可行性红队 | 击杀 |
| 4.3 / 4.5 HTTP 自监 | 当监制台进程实际未提供 `http://127.0.0.1:4270/api/board`，或该端口已被另一进程占用并返回 HTTP 200 时，`HTTP200` 会把错误服务判为健康；方案既未给出该接口存在及响应身份字段的证据，也没有认证/实例标识，健康判定无法实现为“监制台在岗”。 | 高 | 可行性红队 | 击杀 |
| 4.2 未读账本 / 4.3 轮询 | 当 `未读账本.jsonl` 累积到 2 GB、约 1,000 万行，页面每 3 秒打开或刷新一次状态时，方案没有 offset、增量索引或文件上限，服务端会反复扫描/解析该文件；单台值守机很快把 CPU、内存和响应时间耗尽，且问题随账本寿命线性恶化。 | 高 | 成本红队 | 击杀 |
| 4.3、六.5 mtime 验收 | 当瞭望塔守护进程在测试的 30 秒内正常追加一次 `瞭望塔流水.log` 时，“完整轮取数后瞭望塔下所有文件 mtime 未变”必然失败，即使终端完全只读；为让验收可重复，必须另建隔离部署区、冻结守护进程或逐文件排除并维护清单，测试与取证成本被额外放大。 | 中 | 成本红队 | 击杀 |
| 4.4 / 六.7 自启验收 | 在含空格、中文用户名且已安装旧版任务的机器上，`--install`、登录启动、升级覆盖、`--uninstall` 四个状态组合都要实机验证；方案把验收缩成“查任务存在”，会遗漏任务虽注册但启动失败的主路径，后续每次发布都需人工排障和现场复现。 | 高 | 成本红队 | 击杀 |
| 七.3 清账并发 | 值班员在 5 秒内连点“清账”20 次时，终端会起 20 个 `--ack` 子进程与瞭望塔水位写入竞争；既无去重、串行、退出码呈现，也无失败重试语义，人工必须逐次核对账本和水位，清账量一上来就转化为持续的人工与取证成本。 | 高 | 成本红队 | 击杀 |

第1卷 可行性红队：击杀 4 条  
第2卷 成本红队：击杀 4 条  

总体判断：方案把“读取文件并展示”当作低风险，却没有先锁定输出契约、共享故障域和自启生命周期。当前验收还能让空壳任务与误报健康通过，投入后最可能出现的是值守屏看似在线、实际没有可信观测。

## deepseek

- 立场卷：不变量红队
- 状态：**缺席**（原因：应答里没有文本内容（content 为空））—— 上列立场卷本轮落空
- 耗时：176.5s
