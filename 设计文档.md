# 游戏开发者终端 · 设计文档

对应版本 **0.7.8**（2026-08-30）｜源码 `D:\GitHub\gamedev-terminal`｜部署区 `D:\GitHub\AI-GameStudio\终端\`

---

## 怎么用这份文档

这是一份**给你改的**文档，不是给你看的。改完丢回给我，我照着落地。

为了让我读得出你改了什么，请用这四个记号（写在行首或段末都行）：

| 记号 | 意思 | 我会做什么 |
|---|---|---|
| `【改】` | 现在这样不对，应该是…… | 按你写的改，改前如有代价我先说 |
| `【加】` | 缺一个东西 | 先出方案给你点头，再动手 |
| `【删】` | 这个不要了 | 连同它的判据、配置、落盘一起清 |
| `【问】` | 你不确定，要我先答 | 我只回答，不动手 |

**没有记号的段落我一律当成「现状确认无误」**，不会碰。

每节末尾有两栏固定的：

- **可改的旋钮** —— 改配置文件就行，不用打包，多数热生效
- **已知不足** —— 我知道它不好但还没修的；你可以在这里加 `【改】` 给它排序

---

## 一、这是什么

**一块常驻在显示器上的值班屏。**开机自启、整天开着，让你抬眼就知道产线此刻什么状况，低头就能跟坐席说话。

它**不是**：

- 不是 IDE，不写代码
- 不是项管系统——工单、台账、派单都在**监制台**（`127.0.0.1:4270`），终端只读它、不改它
- 不是聊天应用——对话是手段，"知道现在什么情况"才是目的

三条立身原则，后面所有设计都从这三条推出来：

1. **抬眼可见 · 不用点。**要点开才看得到的信息，等于不存在。左右两栏常驻不折叠就是这条。
2. **机器写的做成格子，坐席写的原样呈现。**绝不解析坐席的散文去提取数字——*一个会因为措辞变化而悄悄变错的数字，比没有这个数字坏得多。*
3. **失效方向朝响。**判断不了的时候宁可多报一次，不可以静默吞掉。

---

## 二、形态与骨架

### 2.1 进程形态

一个 Electron 壳，**壳里自带一个 express 服务**（不是连远程的）：

```
游戏开发者终端.exe
  └─ Electron 主进程 (main.js)
       ├─ BrowserWindow → http://127.0.0.1:4280
       └─ express (server.js) ← 端口被占就往后找空的
```

单实例：第二次双击不起第二个服务，把已开的窗拉到前面。

### 2.2 数据根 —— 四级回落

**这是全项目最容易出事的一处**，单独说。终端把数据写在哪，按这个顺序定：

| 顺位 | 取法 | 用于 |
|---|---|---|
| ① | 环境变量 `TERMINAL_ROOT` | 测试时钉死，防止测试污染生产目录 |
| ② | `PORTABLE_EXECUTABLE_DIR` | **打包后的正路**：被双击的那个 exe 所在目录 |
| ③ | `execPath` 的目录 | 其它打包形态（dir/nsis） |
| ④ | `__dirname` | 源码态 = 仓根 |

**为什么不能用 `cwd` 或 `__dirname`：**portable exe 会先把自己解压到 `%TEMP%\ns<随机>.tmp\app\` 再从那里起进程，**退出时整个删掉**。用 cwd 的话数据全写进临时目录，重启即蒸发——而且不报错，因为目录确实存在、写入确实成功。08-28 就这么把 202 条情报数据写丢过。

实际数据根：`D:\GitHub\AI-GameStudio\终端\`

```
终端\
  游戏开发者终端 0.7.8.exe     ← 活体
  .session.json                会话续接 ID
  用量.jsonl                   token 账（按来路分）
  自启-回执.json               --install 的结果（GUI 无控制台，必须落盘）
  班次.json                    班次配置（热生效）
  班次\
    索引.jsonl                 结构化班次记录
    2026-08-30-晨报.md         班次报告正文
  data\                        情报数据
```

### 2.3 打包与换装

```bash
npm run dist
```

输出到 `D:/terminal-build/dist`（**不能放桌面**，会被同步工具搅）。换装流程：按端口找到占用进程杀掉 → 拷新 exe 到部署区 → 起新的 → `curl /health` 看版本号确认。

`signAndEditExecutable: false` 是刻意的，别打开。

### 2.4 登录自启

```bash
游戏开发者终端.exe --install            # 注册开机自启
游戏开发者终端.exe --uninstall
游戏开发者终端.exe --autostart-status
```

**换装后必须重跑 `--install`**，否则自启还指着旧版本 exe。周末巡检第六项专查这个。

### 2.5 窗口形态

两态，界面右上角切：

- **全屏工作台** —— 开窗即最大化，菜单栏自动隐藏
- **半屏情报塔** —— 贴屏右缘的一根竖条（宽度 = 屏宽 22%，钳在 360–460px），**置顶**，可与 Unity/浏览器并排

半屏为什么置顶：不置顶的话点一下 Unity 它就沉下去，那就不叫常驻了。全屏态不置顶，免得挡别的窗。

### 2.6 快捷键

| 键 | 作用 |
|---|---|
| `F11` | 切全屏 |
| `Esc` | **仅全屏时**退出全屏；非全屏时放行给页面 |
| `Ctrl+R` | 重载（清缓存） |

**这三个键走 `before-input-event`，绝不用 `globalShortcut`。**

> 案源 08-30：原实现用 `globalShortcut.register`，那是**系统级独占注册**——只要终端在跑，Esc/F11/Ctrl+R 在**所有程序**里都是死的。加上 08-29 挂了开机自启，症状变成「重启没用、换键盘没用」，完美伪装成硬件故障。守卫在 `test/快捷键.test.js`，想用 globalShortcut 得先把 import 加回去，那时先回答：*这个键值得从整台机器上抢走吗？*

---

## 三、界面结构

### 3.1 主页三栏

```
┌───────────────────────────────────────────────────────────┐
│ 徽标  [对话][日报][原始流][监视][班次][存照][文稿]  钟·形态 │
├──────────┬─────────────────────────────┬──────────────────┤
│  人闸     │  座条（八席在座）            │  产线脉搏         │
│  队列     ├─────────────────────────────┤  计格            │
│          │                             │                  │
│  ①…      │  话流 / 视图区               │  在跑的           │
│  ②…      │                             │                  │
│  ③…      │                             │  近事            │
│          ├─────────────────────────────┤                  │
│  塔况     │  输入区（带单 · @ 点名）      │                  │
└──────────┴─────────────────────────────┴──────────────────┘
```

**左右两栏常驻不可折叠**，因为原则一：要点开才看得到的信息等于不存在。

### 3.2 主页与页签的关系

**页签不是跳转，是在中栏原地换视图。**点「班次」时话流 `hidden`、视图区显示；左右两栏、输入框、座条**全都还在**。

技术上：页面向 `/班次?frag=1` 取片段，中间件把整页的 `<main class="wrap">` 剥出来塞进视图区。URL 走 `/?v=shift`（不是 `/shift`），这样 F5 刷新回来还在壳里。

七个页签的单一事实源是 `server/render/页签.js`——**前端不许自己维护一份名单**：

```js
[ talk / digest / stream / watch / shift / chat / doc ]
```

同一批页面还能**独立直达**（`/digest`、`/shift`……），那时是完整一页带 `← 主页`。这条路留着是为了给班次报告发链接、给瞭望塔弹通知点开。

---

## 四、功能逐项

### 4.1 坐席对话

**这是终端的主功能。**输入框里说话，接的是 Claude Agent SDK，**带工具**——它能读文件、跑命令、查接口。

- 会话续接：`.session.json` 存 session id，重启后接着上次说
- 流式：SSE，边想边吐字
- **放行工具白名单**在 `studio.config` 的「执行器.放行工具」里，`settings.json` 不生效
- 禁用工具硬编码：`rm`/`del`/`rmdir`/`format`/`reg`/`git push`/`git reset --hard`/`shutdown`/`taskkill`
- **必须注入代理 env**（`http://127.0.0.1:7890`），同时 `NO_PROXY=127.0.0.1,localhost,::1`——不然它 curl 本地端口全是 502

**带单**：左栏人闸条目可以「带」进输入框，说话时自动附上单号上下文。

#### 可改的旋钮
- 放行/禁用工具清单
- 系统提示词（现在写在 `坐席选项()` 里，**未外置**）
- 代理地址

#### 已知不足
- 系统提示词硬编码在代码里，改一个字要重新打包
- 没有多会话——只有一条线，说岔了没法开新线程

---

### 4.2 八席与 @ 路由

座条上八个席位，**两席接模型**（助理、总监），其余六席登记但不接：

| 席位 | 接模型 | 人设 |
|---|:---:|---|
| 助理 | ✓ | 协调各主管，把此刻该看的事实呈给制作人 |
| 总监 | ✓ | 从项目全局给出判断与建议，但不直接改台账 |
| 终端项管 | — | 汇总跨项目进度与风险，只看不改排程 |
| 情报主管 | — | 整理技术、工具与行业情报 |
| 财务 | — | 跟踪额度、时间与返工成本 |
| 市场 | — | 基于情报分析市场与玩家 |
| 营销 | — | 同行、曝光与发售路线建议 |

不点名 = 相关席应答；`@某席` = 只找那一位。

**群聊席不带工具**——它们只给意见，不执行动作、不改账。*群里发言默认是「意见」不是「动作」*，真要改账仍走 `/api/act/*` 落台账。

单一事实源 `server/lib/坐席.js`，界面、@ 路由、调用层共用同一份名单。

#### 可改的旋钮
- 席位名单与人设（`坐席.js` 的 `全部` 数组）
- 哪几席接模型（`接模型: true`）

#### 已知不足
- 六席"登记但不接"——点了没反应，界面上看不出区别
- 群聊没有多轮：每条意见都是独立一次调用，席与席之间不互相看见

---

### 4.3 人闸队列（左栏）

从监制台 `/api/attn` 取，**是这块屏最重要的一栏**：等你签字、拍板、废弃的事。

每条带：归属、落点、单号、**停摆时长**。停摆时长是关键——它让「又老了一天」这件事变得可见。

数字键 `1-9` 直接选中左栏对应条目。

#### 可改的旋钮
- 监制台地址（默认 `127.0.0.1:4270`）

#### 已知不足
- 排序规则跟着监制台走，终端这边没法自己调
- **没有分级**：急件和常件混在一列里，只有停摆时长能区分。08-29 写过一版《信件分级重构方案》，结论是改成"信箱降噪"，**未落地**

---

### 4.4 产线脉搏（右栏）

三块：

1. **计格** —— 各状态计数（在途/初检/核查/仲裁/拒 等，H108 十二态的聚合）
2. **在跑的** —— 此刻正在执行的单
3. **近事** —— 最近发生的事件流

数据来自监制台 `/api/board` 与 `/api/pulse`。

#### 已知不足
- 「近事」没有分级，什么都往里塞
- 计格的分组是我定的，没跟你确认过哪几个该并、哪几个该拆

---

### 4.5 凭据寿命

顶部条，读 `claudeAiOauth.expiresAt`，快过期时变色提醒。

**只读过期时刻，从不碰 token 本身。**

---

### 4.6 额度水位

顶部条显示 claude 的 5 小时窗与周窗 utilization。数据来自瞭望塔的 `额度读数.jsonl`（塔每 2 分钟写一次）。

**这个读数同时是班次的开工闸之一**，见 4.10。

#### 已知不足
- 只显示 claude；codex/deepseek 的额度没有通道

---

### 4.7 情报管线

**四个阶段的纯函数管线**，每段可独立测：

```
sources.json → reader（抓） → scoring（评分） → dedupe（去重） → assemble（装配） → 日报
```

**评分规则全在 `config/scoring.json`**，引擎是纯函数：

- **源基础分**：S=50 / A=30 / B=15
- **关键词分**：`slg`/`4x`/`grand strategy` 各 25，`civilization`/`paradox` 20，`systems design`/`postmortem` 15……命中即加、可叠加、按词边界
- **新鲜度分**：满分 20，半衰 36 小时；无发布时刻按 5 分
- **入选两规则**：S 档降门槛全入候选（*垂直深挖*）；A/B 档按总分取 top12（*主流广覆盖*）；日报上限 18 条

判据 C3 会改这个文件的权重再断言入选集合随之变化——**集合不变就是引擎没接配置，必红**。

**调度**：每分钟一拍对时刻表，不用"上次运行 + 间隔"（重启会把间隔重新开始算，那正是漏跑的来源）。时钟可注入，判据能模拟走完一天。

**四个源**（`config/sources.json`，正本是 `docs/情报源清单-草案.md` 的 20 源全集）：

| 源 | 档 | 状态 |
|---|:---:|---|
| Game Developer | A | **HTTP 403 全败** |
| arXiv cs.AI（关键词过滤） | B | 正常 |
| itch.io 新游与工具 | B | **连接超时全败** |
| explorminate | S | **成败均为 0，像是从未被调度** |

**页面**：`/digest`（日报，成品）与 `/stream`（原始流，抓到的全量），都带日期参数。

#### 可改的旋钮
- `config/scoring.json` 全部权重与门槛
- `config/sources.json` 增删源、改档位、关键词过滤
- 抓取时刻表

#### 已知不足
- **四个源里两个全败、一个从未跑过**——实际只有 arXiv 一个源在产出
- 只有 RSS 适配器（`adapters/rss.js`），页面适配器（游戏葡萄公众号、P社 Dev Diaries）没做
- 源失败不报警，只是安静地什么都不产出

---

### 4.8 监视页

配置驱动的健康看板，`监视器/监视器.json` 定义看什么。现有六项：

| 监视项 | 看的是 |
|---|---|
| 瞭望塔 | 心跳是否新鲜 |
| 监制台 | 服务在不在 |
| 今天扶起过谁 | 互保环今日战果 |
| 今天有没有情报 | 情报管线是否产出 |
| 等着你看的 | 未读账本水位 |
| 产线刚发生了什么 | 事件流 |

引擎支持增量读（记 offset，不重读整个文件）与表达式判定。

#### 可改的旋钮
- `监视器/监视器.json` 增删监视项、改阈值与判定表达式

---

### 4.9 席间存照

`/chat` —— 把坐席会话的关键片段留档。区分**播报**（状态广播）与**产出物**（真东西），末行超过 5 分钟没动算陈旧。

---

### 4.10 班次 —— 无人值守自动开工

**这是 08-29 拍板的东西，替代了原来的"事件唤醒哨"方案。**

> 撤案理由：事件触发唤起坐席会自激成环，而且记录里根本没有"缺诊断者"这类故障——**没有它要解决的问题**。改成时钟触发。

**四班**（`班次.json`，改文件热生效，不用打包）：

| 班次 | 到点 | 补跑窗口 | 只在 | 查什么 |
|---|---|---|---|---|
| 夜间巡检 | 02:00 | 120 分 | 天天 | *此刻还活着吗* —— 三方环、自启核验、判据、用量、异常 |
| 晨报 | 09:00 | 180 分 | 天天 | *昨夜干成了什么、今天什么等你* —— H76 固定七段 |
| 夜报 | 23:26 | 90 分 | 天天 | *今天干成了什么、明天从哪接* —— 四段，重收口与交接 |
| 周末巡检 | 10:00 | 240 分 | 周六日 | *攒了一周才看得出来的* —— 漂移、积压、没人管的角落 |

**分工是刻意错开的**——重复等于白烧一班额度。周末巡检专查日班查不到的：债的老化（停摆超一周的）、告警闭环（开了口没合上的）、情报源健康（连续一周零成功的）、判据基线漂移（*套件数只该增不该减*）、磁盘堆积、自启漂移、仓库卫生。

#### 双闸 —— 两个分开的函数

```
可否开班 = 自家账（用量.jsonl 今日已耗 < 120000）
         ∧ 真实窗口水位（额度读数.jsonl 的 5 小时窗 < 70%，且读数 < 15 分钟）
```

**为什么分成两个函数**：不给"默认关的安全检查"留位置。日上限 120k 是算出来的——四班约 23k+24k+35k，60k 会在第三班被自己挡下。

读数陈旧阈值 900 秒：塔每 2 分钟写一次，实测 3485 条读数中位数 120 秒。**陈旧读数不当成当前**（这条是评审击杀出来的）。

#### 十态 —— 互不重叠

`停用 / 已跑 / 未收尾 / 运行中 / 断了 / 被闸挡 / 今天不跑 / 待跑 / 补跑中 / 错过`

「运行中」是必须的：一班要跑 5–7 分钟，不给它自己的态就只能假标成「待跑」或「未收尾」。

#### 三条被评审打出来的纪律

1. **开班与收班各记一条**——只记收班的话，「跑到一半被杀」≡「压根没开」
2. **`今日跑过` 读索引不读文件名**——改班次名会导致重复跑一次 20k
3. **不能写 `is_error && !报告`**——坐席从第一轮就吐字，报告非空，短路会把中途失败记成正常收尾

#### 班次的纪律（写在提示词里）

- **只读不改**，不编辑文件、不跑改动命令
- **不许代签任何人闸**——待验收签字、废弃、品味判断、方向拍板一律只列不签
- 每条结论附**实际看到的证据**：绝对路径、行号、命令输出片段
- 查不到的明写"查不到，因为我没有 X"——**说查不到是合格答案，编一个是不合格的**
- 末尾单列"我没能确认的"，**不许空着写"都确认了"**
- 去戏剧化

#### 可改的旋钮
- `班次.json`：增删班次、改到点、改补跑窗口、改星期、**改提示词全文**
- 日 token 上限、窗口水位线、读数陈旧阈值（现在在 server.js，**未外置**）

#### 已知不足
- **夜报与周末巡检的提示词是我写的，你没过目过**
- 三个闸值（120000 / 70% / 900秒）硬编码在代码里
- 塔的 09:03 晨报 / 23:26 晚报时钟还在弹通知，**与终端的晨报/夜报重复**

---

### 4.11 互保环

三方互相守望，谁倒了别人扶：

```
瞭望塔 ⟷ 监制台
   ↕        ↕
     终端
```

**扶起手段是 `schtasks /Run /TN "任务名"`**，不是自己拼路径起进程——任务名不依赖任何相对路径，也不会因换装而失效。

（中文任务名必须走 `chcp 65001`，否则 Windows 按代码页 936 编码 argv，schtasks 找不着。）

有退避表和上限，扶不动会报"停手"而不是无限重试。

#### 已知不足
- 塔重启后**忘记自己报过失联**——两条 08-29 的告警（03:36 监制台失联、03:52 值守塔阵亡）至今没有恢复行，而两个服务事实上都活着

---

### 4.12 文稿台（2026-08-31 落地）

第七个页签。**948 份 md、五个根**，读 + 编辑 + 记号 + 转交。

```
┌──────────────┬────────────────────────────────────┐
│ 文件库        │  设计文档.md      [文件库][编辑]    │
│  搜文件名…    │  改 3  加 1  问 2   [全部交给坐席]  │
│ ▾ 终端仓  27  ├────────┬───────────────────────────┤
│   设计文档.md │ 记号栏  │  正文（渲染，带行号锚点）   │
│ ▾ 工作室 562  │ 改 42行 │                           │
│   ⋯ 只读      │ 加 88行 │                           │
└──────────────┴────────┴───────────────────────────┘
```

**五个根与读写分级**（制作人 08-31 拍板）：

| 根 | 份数 | 写 |
|---|---:|:---:|
| 终端仓 | 27 | ✓ |
| Ticketflow | 156 | ✓ |
| 三国SLG（排除 Library/PackageCache 的 1323 份） | 133 | ✓ |
| 工作室 · 静态区（协议库/白夜馆/岗位协议/想法） | 135 | ✓ |
| 工作室 · **活存储**（回执/完成/归档/journal/台账） | 427 | **✗** |
| 记忆库（所有项目的 memory/） | 70 | ✗ |

活存储禁写不是我发明的，是 `studio.config.json` 的项目注册表自己写的：
*「runner 每拍读写它们，并发写产生的状态错乱 git 也还原不回。」*

#### 记号栏

把散在几百行里的 `【改】` `【加】` `【删】` `【问】` 收成一条可点的带子，点了跳到那一段，
一键连上下文交给坐席。**这是这一页唯一一件外部编辑器给不了的东西。**

**反引号里的不算**——那是在*谈论*记号，不是记号。所以第一页那张说明表不会被当成四条待办。
（这条有案：08-31 的评审就是拿那张表数出「6 个记号」，而正文里当时一个都没有。）

#### 文件锁

**租约制，不是开关。**「改完自己解锁」那一步一定有人做不到（互保环重启、换装杀进程、浏览器崩了），
所以：页面每 15 秒续租，**停止续租 45 秒即失效**。失效方向朝松，不朝死。

第二个超时：**闲置 30 分钟降级为可抢占**。没有它，你点了编辑去睡觉，
页面整夜续租，锁永不过期——在系统看来「正在编辑」和「人走了」一模一样。

**锁落在盘上，不在内存里**：要挡的三个写手里有两个不在终端进程内（坐席子进程、02:00 的班次）。
坐席那边还会收到一条占用告示（读可以、写不行），并且硬闸在服务端——**告示靠自觉，闸不靠**。

解锁请求进**人闸队列**（常驻可见、带等待时长），不弹 Windows 通知——
你大概率正在改那份文档，弹通知打断你改文档是蠢的。

#### 写口的四道闸

终端在这之前**对外纯只读**。加写口是跨门槛的改动，而这块屏无鉴权、开机自启、整天开着。

1. 只收 `application/json`（`text/plain` 是简单请求，不触发预检——**那是攻击入口**）
2. Origin 必须是自家
3. 一次性写令牌（随页面下发，跨站读不到页面就拿不到）
4. 路径校验 + 写权

#### 冲突：三路，不是「是否覆盖」

编辑期间盘上被改 → **拒写**，给出：盘上那版 / 我这版 / **打开时那版的字节**。
不弹「文件已变更，是否覆盖」——*那个框所有人都点确定*。

四个选项：我改了什么 / 别人改了什么 / 保留我的 / 用盘上的。

#### 版本环

**任何一次写入都产生一版，带「谁写的」**（人 / 总监 / 班次:xxx）。
只记你那侧的话，坐席的改动在历史里是隐形的——**假历史比没历史坏**。上限 50 版。

#### 编辑器

CodeMirror 6 + 软换行 + markdown 高亮，左源码右预览（**预览用同一个 `md.js`**，本地渲染不走往返）。
预览里每段旁边四个记号钮；草稿每 800ms 落**服务端**（不落 localStorage——
4280 被占时服务顺延到 4281，origin 一变 localStorage 就找不着了）。

#### 可改的旋钮
- `<终端根>/文稿根.json` 整表覆盖根表（热生效，照 `班次.json` 的成例）
- 租约 45 秒 / 闲置 30 分钟 / 版本上限 50（在 `server/lib/文锁.js` 顶部，**未外置**）

#### 已知不足
- 三个闸值硬编码
- 记号栏的 DOM 渲染**没有判据**（无头环境不派发 rAF，验了就是假绿或假红）——明写的盲区
- 记忆库那 70 份是**所有项目**的 memory/，路径丑
- 编辑器包 543 KB，多了一个构建步骤（有判据盯着「忘了重新构建」）

---

### 4.13 用量账

`用量.jsonl` 逐次记录，**按来路分**：

- `人` —— 你在输入框里的交互
- `班次:晨报` / `班次:夜间巡检` …… —— 无人值守

开班闸只数 `班次` 开头的——你自己聊天不该把班次挤掉。

---

## 五、跨切面

### 5.1 判据体系（H104）

**13 个判据文件、约 180 项断言，红 0。**

| 文件 | 项 | 守什么 |
|---|---:|---|
| 班次.test.js | 46 | 双闸、十态、挑班、时刻判定 |
| 监视取数.test.js | 22 | 增量读、表达式判定 |
| 存照.test.js | 20 | 播报/产出物识别、陈旧判定 |
| 互保.test.js | 16 | 扶起、真死判定、退避 |
| m1c.test.js | 16 | 情报管线端到端 |
| 群聊.test.js / 自启.test.js | 12 / 12 | @ 路由 / 计划任务注册 |
| m1b.test.js | 11 | 评分与入选 |
| 壳.test.js | 9 | 窗口形态 |
| m1a.test.js / 主页群聊.test.js | 6 / 6 | 抓取 / 主页集成 |
| 快捷键.test.js | 4 | **不许抢全局键** |
| packaged-root.test.js | — | 打包后数据根 |

**规矩**：判据必须**验行为**且**自证能红**——改坏实现，判据必须变红，再改回来。`test/自证能红.js` 里维护着 30+ 个变异。

*grep 源码文本不算判据。*唯一例外是快捷键那组：要守的不变量是"一个函数一次都不许被调用"，坏结果发生在被测进程之外（整台机器的其它程序），行为判据够不着。

> `自启.test.js` 会真注册一条计划任务（自己清理）。班次提示词里明写让坐席跳过它。

### 5.2 视觉系统

暗色，OKLCH 色板，token 在 `public/tokens.css`：

```
--ground  0.155  底
--panel   0.185  面
--raised  0.215  抬起
--ink     0.93   正文        --ink-2 0.78   --ink-3 0.66
--gate    0.78 0.145 72°     人闸橙（唯一的强调色）
--live    0.74 0.13 158°     在跑
--warn    0.72 0.13 62°
间距 4/8/12/16/24/32
```

**取向**：值班屏，长时间开着，暗色不刺眼。橙色只给人闸——*屏幕上唯一会主动要你动手的东西*。

几条硬规矩：

- **不用 emoji 当图标系统**——状态标记都是画出来的 SVG 形状
- **不做等大卡片网格**当页面结构（班次页是按时间排的条带，不是格子）
- 对比度验到 ≥4.5:1（用 canvas `getImageData` 量，不解析计算样式字符串）
- 正文 65–75ch

### 5.3 落盘清单

| 文件 | 谁写 | 内容 |
|---|---|---|
| `用量.jsonl` | 终端 | 每次调用的 token 与来路 |
| `班次/索引.jsonl` | 终端 | 开班/收班记录 |
| `班次/<日期>-<班次>.md` | 终端 | 班次报告正文 |
| `.session.json` | 终端 | 会话续接 |
| `自启-回执.json` | 终端 | `--install` 结果 |
| `data/` | 终端 | 情报数据 |
| `额度读数.jsonl` | 瞭望塔 | 5 小时窗/周窗 utilization |
| `未读账本.jsonl` | 瞭望塔 | 未读事件 |

### 5.4 硬纪律（改设计时不许破的）

- **H57**：总监不直接造执行单，需求走 `/api/pm/draft` 派单委托
- **H104**：判据必须验行为 + 自证能红
- 班次坐席**只读不改，不代签人闸**
- 凭据接口只读 `expiresAt`，**从不碰 token 本身**
- 中文指令绝不走 argv（走 stdin 或 `--data-binary @file`）
- 改文件一律 Write/Edit，**禁 `node -e` / `sed -i` 内联**（反引号、`${}`、`\n`、反斜杠会被 shell 吃掉，且**多半不报错、静默写坏**）

---

## 六、已知欠账

按我的判断排序，你可以改序号或标 `【删】`：

| # | 欠账 | 影响 |
|---:|---|---|
| 1 | 情报四源里两源全败、一源从未调度 | 情报功能实际只有 1/4 在跑 |
| 2 | 夜报与周末巡检提示词你没过目 | 两班每天在按我写的口径产出 |
| 3 | 塔的晨报/晚报时钟与终端班次重复弹通知 | 同一件事通知两次 |
| 4 | 两条告警从未闭环（08-29 03:36 / 03:52） | 看板上永远挂着假故障 |
| 5 | 人闸队列没有分级 | 急件常件混排，只能靠停摆时长分辨 |
| 6 | 班次三个闸值硬编码 | 调一个数要重新打包 |
| 7 | 坐席系统提示词硬编码 | 同上 |
| 8 | 仓根有 141MB 未跟踪 exe，`.gitignore` 不挡 `*.exe` | 一次 `git add -A` 就永久写进历史 |
| 9 | 终端仓只有 1 个提交、47 项未入库 | 没有版本history可回溯 |
| 10 | `D:/terminal-build/dist` 历史 exe 约 1.4GB 无上界增长 | 磁盘 |
| 11 | 只有一条会话线，无法开新线程 | 说岔了没法另起 |
| 12 | 红队异议：`/shift` 该并进主页而非单开一页 | 你未裁决 |

---

## 七、你要加的

> 这一节是空的，留给你写。想到什么写什么，不用管格式——写「我希望能……」就行，我来翻译成方案。

### 想加的功能


### 想改的地方


### 用着别扭的地方


---

*文档止于此。改完丢回给我。*
